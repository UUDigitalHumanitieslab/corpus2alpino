language: python
python:
  - "3.6"
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libfolia-dev libticcutils2-dev libucto-dev ucto
install:
  - pip install -r requirements.txt
script:
  - python -m unittest
